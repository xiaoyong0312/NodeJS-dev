/**
 * @desc 登录,登出 模块;
 */

'use strict';
var util = require('../../utils/util');
var type = require('../../utils/util').type;
var isNull = util.isNull;

var Sign = module.exports = {};

console.log('this is sign.js ----');
console.log('this is sign.js ----' + isNull('abcd'));

Sign.SignIn = (userName, passWord)=> {
  if (userName) {
  }
}


Sign.SignOut = (userName, passWord)=> {
  if (userName) {
  }
}
